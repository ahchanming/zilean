#include "dispatch_service.h"
#include <stdlib.h>
#include <stdio.h>

void DispatchService::Dispatch(int length, int type, char* byte){
	switch (type){
		case 1000:
			break;
		default:
			printf("No one can match!");
	}
		
}
